@model ProjetoFormulario.Models.Cliente
@{
    ViewData["Subtitle"] = "Editar Cliente";
}

<h2 class="titulo">@ViewBag.Subtitle</h2>
<hr />

<div class="card bg-light p-5">
    <form asp-controller="home" asp-action="editarCliente">
        @Html.HiddenFor(Model => Model.Id)
        <div class="form-group">
        @Html.LabelFor(model => model.Nome)
        <br />
        @Html.EditorFor(model => model.Nome)
        </div>

        <div class="form-group">
        @Html.LabelFor(model => model.Telefone)
        <br />
        @Html.EditorFor(model => model.Telefone)
        </div>
        <a asp-controller="home" asp-action="index" class="btn btn-primary text-decoration-none m-2">Cancelar</a>
        <button class="btn btn-primary">Salvar</button>

    </form>
</div>

